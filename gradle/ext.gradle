
ext {
    gradle_version = '3.5.3'
    bytex_version = "0.3.0"

    upload_group = 'com.rookie.plugin'
//    upload_version = "1.0.0${(useSnapshotMaven.toBoolean() ? "-${doSh("git config user.name")}-SNAPSHOT" : "")}"
    upload_version = bytex_version //与 byteX 版本保持一致
    upload_dir = project.rootProject.file('gradle_plugins').path

}



